{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "export PNPM_CONFIG_STRICT_DEP_BUILDS=false PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false && pnpm --filter @party-hub/shared run build && pnpm --filter @party-hub/frontend run build",
  "installCommand": "export PNPM_CONFIG_STRICT_DEP_BUILDS=false PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false && pnpm install --frozen-lockfile",
  "outputDirectory": "packages/frontend/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
